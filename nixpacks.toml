[phases.setup]
nixPkgs = ['nodejs_18', 'npm']

[phases.install]
cmds = ['npm ci']

[phases.build]
cmds = ['npx openclaw doctor --fix || true']

[start]
cmd = 'npm start'

[variables]
NODE_ENV = 'production'
PORT = '18789'
